{% extends "base.html.twig" %}

{% block title %}
    Modification du mot de passe
{% endblock %}


{% block content %}
<div  id=""   class="container">
    <div class="row">
        <div class="col-8 mx-auto bandeau">
            <h3 class="my-4 text-center">
                Modifier mon mot de passe
            </h3>
            {{ form(form) }}
            <a href="{{ path('home') }}" class="switch">
                <button  type="button" class="btn btn-block btn-outline-billy">Retour</button>
            </a>
        </div>
    </div>
</div>
{% endblock %}